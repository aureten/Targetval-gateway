# TargetVal Gateway — runtime deps (math synthesis + FastAPI)
# Pinning to avoid CLI incompatibility with h11 options and to ensure SciPy wheels.

# Core
fastapi>=0.112,<0.116
uvicorn[standard]>=0.30,<0.31
httpx>=0.27,<1.0
pydantic>=2.5,<3.0

# Math / synthesis
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3

# Optional but recommended
networkx>=3.1
orjson>=3.9

# Transport stack (keep in sync with uvicorn)
h11>=0.14
